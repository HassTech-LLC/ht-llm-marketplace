# Compatibility Scorecard

Current claim: generated by `/api/compatibility/scorecard`; the repository default remains locked below `best-replacement` until every evidence gate passes.

| Competitor | Covered | Gaps |
|---|---|---|
| Ollama | Pull/run/list foundation, API aliases, Ollama-library GGUF import | Full model lifecycle parity and broad client smoke matrix |
| LM Studio | Local GGUF discovery, load/unload foundation, document chat scaffold | Production desktop installer, richer RAG citations, full OpenAI parity proof |
| Jan | Local-first UI, CLI, model manager foundation | Installer and extension parity |
| LocalAI | OpenAI-compatible chat, Responses wrapper, embeddings endpoint contract | Multimodal, real embedding backend enabled by default, deployment matrix |
| Open WebUI | Marketplace UI, runtime manager, document workspace scaffold | Multi-user server/admin feature set |
| llama.cpp | Direct GGUF runtime, local streaming chat, delegated server status scaffold | Continuous batching and delegated server binary proof |
| KoboldCpp | Local GGUF runtime foundation | Story/chat preset compatibility |

Proof surfaces:

- Studio `Proof` tab
- `npm run check:compatibility`
- `npm run scorecard:gate`
- `npm run smoke:installer`
- `npm run smoke:studio`
- `HT_DOCKER_SMOKE_REQUIRED=1 npm run smoke:docker`
- `npm run peak:smoke`

This scorecard remains below `best-replacement` until generated compatibility smoke, benchmark evidence, package smoke, installer smoke, Studio smoke, and Docker smoke support a higher claim.
